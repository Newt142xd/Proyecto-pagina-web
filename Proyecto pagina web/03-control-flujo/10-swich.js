let accion = "Actualizar";

switch (accion) {
    case "listar":
        console.log("Accion de listar");
        break;
    case "guardar":
         console.log("Accion de guardar");
         break;
    default:
        console.log("Accion no reconocida")
}
// se puede utilizar switch o if else, esto depende porque con switch se esta comparando con la bariable de arriba
// Eso depende de preferencia y utilizar los dos  